# dbrecover

## Abstruct
This MariaDB recovery script is based on CentOS 7. The internal shell script is written in bash.

## Setting

1. composer install
````
$ composer install
````

2. .env setting
````
````
